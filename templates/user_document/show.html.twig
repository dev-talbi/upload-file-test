{% extends 'base.html.twig' %}

{% block title %}UserDocument{% endblock %}

{% block body %}
    <h1>UserDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_document.name }}</td>
            </tr>
            <tr>
                <th>Upload</th>
                <td>{{ user_document.upload }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_document_index') }}">back to list</a>

    <a href="{{ path('user_document_edit', {'id': user_document.id}) }}">edit</a>

    {{ include('user_document/_delete_form.html.twig') }}
{% endblock %}
